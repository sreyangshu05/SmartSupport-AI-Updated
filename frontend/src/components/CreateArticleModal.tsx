import { useState } from 'react';
import { X } from 'lucide-react';
import { useApp } from '../context/AppContext';
export default function CreateArticleModal({onClose}:{onClose:()=>void}){const {addKBArticle,categories}=useApp();const [title,setTitle]=useState('');const [content,setContent]=useState('');const [summary,setSummary]=useState('');const [categoryId,setCategoryId]=useState('');const [tags,setTags]=useState('');const [publish,setPublish]=useState(false);const [saving,setSaving]=useState(false);const [error,setError]=useState<string|null>(null);const submit=async(e:React.FormEvent)=>{e.preventDefault();setSaving(true);setError(null);try{await addKBArticle({title,content,summary:summary||undefined,categoryId:categoryId||undefined,tags:tags.split(',').map(x=>x.trim()).filter(Boolean),isPublished:publish});onClose()}catch(err){setError(err instanceof Error?err.message:'Could not create article')}finally{setSaving(false)}};return <div className="fixed inset-0 z-50 grid place-items-center bg-black/50 p-4"><form onSubmit={submit} className="max-h-[90vh] w-full max-w-3xl overflow-y-auto rounded-xl bg-white shadow-2xl"><header className="sticky top-0 flex items-center justify-between border-b bg-white px-6 py-4"><h2 className="text-xl font-semibold">Create article</h2><button type="button" onClick={onClose} className="rounded p-2 hover:bg-gray-100"><X size={20}/></button></header><div className="space-y-4 p-6"><label className="block text-sm font-medium">Title *<input value={title} onChange={e=>setTitle(e.target.value)} minLength={3} required className="mt-1 w-full rounded-lg border px-3 py-2"/></label><label className="block text-sm font-medium">Summary<input value={summary} onChange={e=>setSummary(e.target.value)} className="mt-1 w-full rounded-lg border px-3 py-2"/></label><label className="block text-sm font-medium">Content *<textarea value={content} onChange={e=>setContent(e.target.value)} minLength={10} required rows={12} className="mt-1 w-full rounded-lg border px-3 py-2 font-mono"/></label><div className="grid gap-4 sm:grid-cols-2"><label className="text-sm font-medium">Category<select value={categoryId} onChange={e=>setCategoryId(e.target.value)} className="mt-1 w-full rounded-lg border px-3 py-2"><option value="">None</option>{categories.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select></label><label className="text-sm font-medium">Tags<input value={tags} onChange={e=>setTags(e.target.value)} className="mt-1 w-full rounded-lg border px-3 py-2" placeholder="password, security"/></label></div><label className="flex items-center gap-2 text-sm"><input type="checkbox" checked={publish} onChange={e=>setPublish(e.target.checked)}/>Publish immediately</label>{error&&<p className="rounded-lg bg-red-50 p-3 text-sm text-red-700">{error}</p>}</div><footer className="flex gap-3 border-t px-6 py-4"><button type="button" onClick={onClose} className="flex-1 rounded-lg border px-4 py-2">Cancel</button><button disabled={saving} className="flex-1 rounded-lg bg-blue-600 px-4 py-2 text-white disabled:opacity-50">{saving?'Saving…':'Create article'}</button></footer></form></div>}
