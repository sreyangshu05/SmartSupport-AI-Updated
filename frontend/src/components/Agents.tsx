import { Activity, Mail, Users } from 'lucide-react';
import { useApp } from '../context/AppContext';
export default function Agents(){const {agents}=useApp();return <div className="space-y-6"><div className="grid gap-6 md:grid-cols-2 xl:grid-cols-3">{agents.map(a=><article key={a.id} className="rounded-lg border bg-white p-6"><div className="mb-4 flex items-start gap-4"><div className="grid h-14 w-14 place-items-center rounded-full bg-blue-600 text-lg font-bold text-white">{a.fullName.split(' ').map(x=>x[0]).join('').slice(0,2)}</div><div className="min-w-0"><h3 className="truncate text-lg font-semibold">{a.fullName}</h3><p className="capitalize text-sm text-gray-600">{a.role.replace('_',' ')}</p><p className={`mt-1 inline-flex items-center gap-1 rounded-full px-2 py-0.5 text-xs ${a.isActive?'bg-green-100 text-green-700':'bg-gray-100 text-gray-600'}`}><Activity size={12}/>{a.isActive?'Active':'Inactive'}</p></div></div><p className="flex items-center gap-2 truncate text-sm text-gray-600"><Mail size={16}/>{a.email}</p><div className="mt-5 grid grid-cols-2 gap-3 border-t pt-4"><div><p className="text-xs text-gray-500">Open load</p><p className="text-xl font-bold">{a.openTicketCount||0}</p></div><div><p className="text-xs text-gray-500">Last active</p><p className="text-sm font-medium">{a.lastActiveAt?new Date(a.lastActiveAt).toLocaleDateString():'—'}</p></div></div></article>)}</div>{!agents.length&&<div className="rounded-lg border bg-white p-10 text-center text-gray-500"><Users className="mx-auto mb-3"/>No agents available.</div>}</div>}
